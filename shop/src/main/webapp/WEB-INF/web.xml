<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://xmlns.jcp.org/xml/ns/javaee" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd" id="WebApp_ID" version="3.1">
  <display-name>shop</display-name>
  
	<!-- 
		필터
		javaEE 기술에서는 서블릿의 요청 처리보다 앞선 객체가 지원되는데, 이 객체를 저으이해놓으면 무조건요청이 이 개겣를 거처서 
		간다고 하여 필터라 한다.  
		필터의 주 용도 - 웹애플리케이션 방문시 제일 처음에 거쳐가게 되므로, 접속자 수 통계, 인코딩처리등의 공통적 처리에 사용
		정의시 주의) 서블릿보다 반드시 앞서서 정의해야 필터가 동작함
	-->
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
	</filter>
		
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

	<!-- 
		우리가 제작한 MVC 프레임워크와 마찬가지로, 스프링 MVC 또한 모든 요청을 하나의 진입점으로 몰아넣는다.
		즉, 프런트(맨앞)에 유일한 컨트롤러 하나를 둔다
	 -->
	   <!-- 글 삭제 요청 처리 서블릿  -->
	<servlet>
    	<servlet-name>dispatcher</servlet-name>
      	<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
      
      <!-- init-param 이란 이 서블릿의 인스턴스가 최초에 생성되어질때, 
      		호출되는 생명주기 메서드인 init() 메서드 안으로 전달할 문자열 정보가 있을 경우 이 파라미터 태그를 이용할수 있다.
      		아래의 경우 전통적으로 사용해왔던, xml 방식이 아닌 자바 클래스를 이용한 설정방식을 위한 클래스의 위치를 
      		지정하고 있다.
      		주의) 자바 설정 방식을 사용할 경우 스프링의 컨테이너는 AnnotationConfigApplicationContext 를 사용해야하며
      				그 중 웹애플리케이션에 특화된  AnnotationConfigApplicationContext 를 써야한다.
        -->
		<init-param>
	  		<param-name>contextClass</param-name>
	     	<param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
	    </init-param>
	    
	     <init-param>
	     	<param-name>contextConfigLocation</param-name>
	     	<param-value>com.ch.shop.config.spring.UserWebConfig</param-value>
	     </init-param>
  	</servlet>
  
	<servlet-mapping>
	  	<servlet-name>dispatcher</servlet-name>
	  	<url-pattern>/</url-pattern>
	</servlet-mapping>
	
</web-app>