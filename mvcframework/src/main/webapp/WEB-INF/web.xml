<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://xmlns.jcp.org/xml/ns/javaee" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd" id="WebApp_ID" version="3.1">
  <display-name>mvcframework</display-name>

<!-- 
필터
	javaEE 기술에서는 서블릿의 요청 처리보다 앞선 객체가 지원되는데, 이 객체를 저으이해놓으면 무조건요청이 이 개겣를 거처서 
	간다고 하여 필터라 한다.  
	필터의 주 용도 - 웹애플리케이션 방문시 제일 처음에 거쳐가게 되므로, 접속자 수 통계, 인코딩처리등의 공통적 처리에 사용
	정의시 주의) 서블릿보다 반드시 앞서서 정의해야 필터가 동작함
-->
<filter>
	<filter-name>encodingFilter</filter-name>
	<filter-class>com.ch.mvcframework.web.filter.CharacterEncodingFilter</filter-class>
	<init-param>
		<param-name>encoding</param-name>
		<param-value>UTF-8</param-value>
	</init-param>
</filter>
<filter-mapping>
	<filter-name>encodingFilter</filter-name>
	<url-pattern>/*</url-pattern>
</filter-mapping>

<servlet>
	<servlet-name>dispatcher</servlet-name>
	<servlet-class>com.ch.mvcframework.controller.DispatcherServlet</servlet-class>
	<!-- 이 서블릿이 태어난 직후 호출되는 init()  생명주기 메서드에 전달할 메개변수와 값을 등록  
			그리고 이 파라미터 변수와 값은 init() 메서드의 메개변수로 정의된 ServletConfig 로 전달
	-->
	<init-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>WEB-INF/servlet-mapping.txt</param-value>
	</init-param>
</servlet>
<servlet-mapping>
	<servlet-name>dispatcher</servlet-name>
	<!-- 앞의 어떤 경로가 오든 상관없으며, 단확장자가 .do 이면 무조건  DispatcherServlet 을 만나게 됨 -->
	<url-pattern>*.do</url-pattern>
  </servlet-mapping>
  
   	<resource-ref>
		<description>MySQL DataSource</description>	
		<res-ref-name>jndi/mysql</res-ref-name>
		<res-type>javax.sql.DataSource</res-type>	 
		<res-auth>Container</res-auth>		
 	</resource-ref>
  
</web-app>